PROPOSALES_API_KEY=
PROPOSALES_COMPANY_ID=5265

# OpenRouter (production — routes chat/extraction through single provider)
OPENROUTER_API_KEY=

# OpenAI (always required for embeddings — OpenRouter doesn't serve embedding models)
OPENAI_API_KEY=

# Anthropic (fallback for chat when OPENROUTER_API_KEY is not set)
ANTHROPIC_API_KEY=
